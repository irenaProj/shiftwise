# railway.toml — tells Railway how to build and run the backend
[build]
builder = "nixpacks"
buildCommand = "npm install && npm run db:generate && npm run build"

[deploy]
startCommand = "npm run start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[services]]
name = "shiftwise-api"
